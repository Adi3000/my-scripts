services:
  ffxivv-notifier:
    build:
      context: notifier
      dockerfile: Dockerfile
    env_file: /etc/default/ffxivv
    networks:
    - traefik
    - postgres

networks:
  postgres:
    name: postgres
    external: true
  traefik:
    name: traefik
    external: true
